---

# project information
project_name: obico
project_url: "https://www.obico.io/"
project_logo: "https://www.obico.io/wwwimg/logo.svg"
project_blurb: "[{{ project_name|capitalize }}]({{ project_url }}) - Community-built, open-source smart 3D printing platform used by makers, enthusiasts, and tinkerers around the world."
project_github_repo_url: "https://github.com/hydazz/docker-{{ project_name }}"
project_blurb_optional_extras_enabled: false

# supported architectures
available_architectures:
  - { arch: "{{ arch_x86_64 }}", tag: "amd64-latest"}

# development version
development_versions: false

# container parameters
common_param_env_vars_enabled: true
param_container_name: "{{ project_name }}"
param_usage_include_net: false
param_usage_include_env: true
param_usage_include_vols: true
param_usage_include_ports: true
param_device_map: false
cap_add_param: false

# optional container parameters
opt_param_usage_include_env: false
opt_param_usage_include_vols: false
opt_param_usage_include_ports: false
opt_param_device_map: false
opt_cap_add_param: false
optional_block_1: false

# application setup block
app_setup_block_enabled: true
app_setup_block: |
  Nothing to see here, seeing if creating a signle obico-server image is feasable without rewriting everything.

# changelog
changelogs:
  - { date: "1.02.23:", desc: "Initial Release." }
